/* Replace the previous v19 coach file with this version. */
(()=>{'use strict';const K='power-discipline-v18',q=(s,r=document)=>r.querySelector(s),T=()=>new Date().toISOString().slice(0,10),ago=n=>{let d=new Date;d.setUTCDate(d.getUTCDate()-n);return d.toISOString().slice(0,10)},read=()=>{try{return JSON.parse(localStorage.getItem(K)||'{}')}catch{return{}}},save=x=>localStorage.setItem(K,JSON.stringify(x)),sc=d=>{let a=['focus','energy','action','recovery'].map(k=>+d?.metrics?.[k]).filter(v=>v>=1&&v<=10);return a.length?a.reduce((x,y)=>x+y,0)/a.length/10:null},last=x=>[...(x.days||[])].sort((a,b)=>b.date.localeCompare(a.date))[0],miss=x=>{let d=[...(x.days||[])].filter(d=>sc(d)!==null).sort((a,b)=>b.date.localeCompare(a.date))[0]?.date;if(!d)return 0;return Math.max(0,Math.floor((Date.parse(T()+'T00:00:00Z')-Date.parse(d+'T00:00:00Z'))/86400000))},low=x=>{let a=[...(x.days||[])].sort((a,b)=>b.date.localeCompare(a.date)).slice(0,2);return a.length===2&&a.every(d=>(sc(d)||0)<.5)},move=d=>{if(!d||sc(d)==null)return['Начните с короткой отметки','Заполните четыре шкалы — приложение увидит, где системе нужна поддержка.'];let m=d.metrics||{},a=[['focus','Фокус','Сегодня вечером подготовьте первый час: одно главное действие, закрытые уведомления и готовое рабочее место.'],['energy','Энергия','Завтра не увеличивайте нагрузку: выберите одно ключевое действие и сократите второстепенные задачи.'],['action','Ключевые действия','До начала дня сформулируйте один измеримый результат, который должен быть завершён до отвлечений.'],['recovery','Восстановление','Сделайте восстановление главной задачей: время отбоя, спокойное завершение дня и снижение стимуляторов вечером.']].map(x=>({...{k:x[0],n:x[1],t:x[2]},v:+m[x[0]]})).filter(x=>x.v>=1).sort((a,b)=>a.v-b.v)[0];if(!a)return['Начните с короткой отметки','Заполните четыре шкалы, чтобы увидеть следующий ход.'];return a.v>=7?['Сохраните работающий сценарий','Все четыре фактора удержаны на хорошем уровне. Не повышайте планку: повторите условия, которые сработали сегодня.']:[`Следующий ход: ${a.n.toLowerCase()}`,`${a.n} — ${a.v}/10. ${a.t}`]},css=document.createElement('style');css.textContent='.pd-coach{margin:18px 0 0;padding:19px 20px;border-radius:16px;border:1px solid #d8e2ff;background:linear-gradient(125deg,#edf3ff,#fbfaff);box-shadow:0 10px 24px #3c529615}.pd-coach.return{border-color:#f2d6a6;background:linear-gradient(125deg,#fff8e9,#fffdf8)}.pd-coach-k{font-size:10px;letter-spacing:.11em;text-transform:uppercase;font-weight:900;color:#7180a8}.pd-coach h3{font-size:19px;letter-spacing:-.025em;margin:5px 0}.pd-coach p{margin:0;color:#60708f;max-width:760px}.pd-coach-actions{display:flex;gap:9px;flex-wrap:wrap;margin-top:13px}.pd-coach button{font:inherit;font-weight:800;cursor:pointer;padding:8px 11px;border-radius:9px;border:1px solid #d5dffb;background:#fff;color:#536899}.pd-coach .primary{border:0;color:#fff;background:linear-gradient(120deg,#597af0,#8064e9)}';document.head.append(css);function render(){document.querySelectorAll('.pd-coach').forEach(e=>e.remove());let tab=q('#dashboard.tab.active');if(!tab)return;let hero=q('.hero',tab);if(!hero)return;let d=read(),c=d.coach||{},n=miss(d),ret=c.manualReturn||n>=3||low(d),html;if(ret)html=`<article class="pd-coach return"><div class="pd-coach-k">Режим возврата в систему</div><h3>Не нужно догонять.</h3><p>${n>=3?`Нет отметок ${n} дня. `:'Последние два заполненных дня были в красной зоне. '}Сегодня верните базовый контур: одно главное действие, короткая отметка дня и подготовка восстановления. Остальное — необязательно.</p><div class="pd-coach-actions"><button class="primary" data-c="input">Сделать короткую отметку</button><button data-c="off">Завершить режим</button></div></article>`;else{let [h,p]=move(last(d));html=`<article class="pd-coach"><div class="pd-coach-k">Следующий ход</div><h3>${h}</h3><p>${p}</p><div class="pd-coach-actions"><button class="primary" data-c="input">Открыть отметку дня</button><button data-c="on">Вернуться в систему</button></div></article>`}hero.insertAdjacentHTML('afterend',html);q('.pd-coach',tab).onclick=e=>{let a=e.target.dataset.c;if(!a)return;if(a==='input')q('[data-tab="input"]')?.click();else{let x=read();x.coach=x.coach||{};x.coach.manualReturn=a==='on';save(x);render()}}}document.addEventListener('click',()=>setTimeout(render,0),true);render()})();
